# colomba-swiss.ch

Static landing page for Colomba, served by GitHub Pages from this repository.

A single self-contained `index.html`: no build step, no framework, no external
requests (styles are inline, the favicon is an inline SVG data URI). Edit the
file, commit, and Pages redeploys.
